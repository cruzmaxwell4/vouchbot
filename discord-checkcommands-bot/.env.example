# Get these from https://discord.com/developers/applications
DISCORD_TOKEN=your-bot-token-here
CLIENT_ID=your-application-client-id-here

# Optional: set this to a server (guild) ID while developing for instant
# command updates. Leave blank to register commands globally instead
# (works in every server the bot joins, but can take up to an hour to propagate).
GUILD_ID=
